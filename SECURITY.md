# 安全策略

## 支持范围

安全修复优先进入当前维护的 `main` 分支。部署方应持续使用最新已验证提交，并保持 Docker Engine、PostgreSQL、MinIO、Caddy、Python 和 Node.js 依赖在受支持版本。

## 报告安全问题

发现鉴权绕过、越权访问、Secret 泄露、任意文件访问、上传漏洞或个人数据泄露时，请不要在公开 Issue 中披露利用细节。应通过仓库所有者认可的私有联系方式报告，并提供：

- 受影响版本或 Git commit；
- 复现条件、请求路径和最小复现步骤；
- 可能影响的数据与权限范围；
- 已采取的临时缓解措施。

报告内容不要附带真实用户 Token、微信 Secret、数据库密码、管理员密码或未脱敏的个人信息。

## 部署方责任

- 真实配置只保存在 `deploy/.env` 或受控 Secret 系统中，并限制文件权限。
- 生产环境关闭开发登录，不暴露 PostgreSQL、MinIO Console 和内部 API 端口。
- 为每位管理员分配独立账号和最小学校权限，定期审查禁用账号与审计日志。
- 定期备份 PostgreSQL 和公开/私有 MinIO bucket，并进行恢复演练。
- Secret 疑似泄露时立即轮换，不以“已从 Git 删除”作为处置完成标准。
