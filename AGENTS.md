# 项目协作约定

- 所有新增或更新的说明文档、README、操作指南和代码注释统一使用简体中文。
- 变量名、函数名、API 字段、错误码、命令、路径、协议名和第三方产品名保持原始形式，不做强制翻译。
- `# type: ignore`、`# noqa`、uni-app 条件编译标记等工具指令必须保持原样，避免影响编译、检查或运行。
- 修改说明文档时同步核对内容是否仍与当前代码、部署方式和环境配置一致。
